import MyStack from '../stack'

describe('MyStack', () => {
    it('is a sample test so that you know how tests work', () => {
        const a = 1
        const b = 9

        expect(a).toEqual(b)
    })
})
