import fizzbuzz from '../fizzbuzz'

describe('fizzbuzz', () => {
    it('is a sample test provided for your benefit', () => {
        const a = 1
        const b = 2

        expect(a).toBe(b)
    })
})