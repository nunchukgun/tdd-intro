import fizzbuzz from '../fizzbuzz_solution'

it('returns an empty array if given an empty array', () => {
    const input = []
    const expected = []

    const result = fizzbuzz(input)
    expect(result).toEqual(expected)
})

it('returns the input if it is not fizzbuzz-able', () => {
    const input 
})
